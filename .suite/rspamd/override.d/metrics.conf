# Rspamd metrics configuration for Robin suite testing

# Lower thresholds for easier testing
actions {
  reject = 15;
  add_header = 6;
  greylist = 4;
}

# Add custom symbols for testing
symbol "SPAM_TEST" {
  weight = 10.0;
  description = "Test spam marker";
  one_shot = true;
}

symbol "GTUBE" {
  weight = 20.0;
  description = "GTUBE spam test pattern";
  one_shot = true;
}
