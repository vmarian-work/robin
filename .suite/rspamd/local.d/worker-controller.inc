# Rspamd controller worker configuration for Robin suite testing

bind_socket = "*:11334";

# Password for web UI access (use "robin" as password)
password = "robin";

# Enable learning
enable_password = "robin";
