# Dovecot configuration version (must be first)
dovecot_config_version = 2.4.0
dovecot_storage_version = 2.4.0

# Enable IMAP and LMTP protocols
protocols = imap lmtp

# Include configuration files
!include conf.d/*.conf
